apply plugin: 'java'
defaultTasks 'build'

sourceCompatibility = JAVA_VERSION_SOURCE
targetCompatibility = JAVA_VERSION_CLASSES

dependencies {
	compile project(':devices:common')
    compile project(':devices:x73')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
